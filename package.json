{
  "name": "blog",
  "description": "Blog personal con 11ty + Netlify",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "deploy": "eleventy && npx netlify-cli deploy --prod --dir=_site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
